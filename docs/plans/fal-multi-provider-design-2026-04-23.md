# Design: Artificer Multi-Provider Media Generation Layer

Generated by /office-hours on 2026-04-23 (expanded from v1 on 2026-04-23 evening)
Branch: main
Repo: artificer-mcp
Status: APPROVED (v2 — expanded scope, reviewed 2026-04-23)
Mode: Builder
Source plan: `docs/plans/fal-ai-integration.md` (this design supersedes that plan and widens scope beyond fal to the full multi-provider architecture)
Related artifacts:
  - `docs/plans/fal-q1-research-2026-04-23.md` — architecture screen + infra posture
  - `docs/plans/fal-bakeoff-2026-04-23.md` — talking-head bake-off: Wan 2.7 default (720p + integer duration), Kling AI Avatar v2 Pro as high-res fallback, veed/fabric as fast-preview path (revised 2026-04-24)

## Context

Artificer-mcp is a shipped open-source MCP server that wraps generative media tools for AI callers. Today it covers Google's stack (Veo video, Gemini/Nano Banana image, Lyria music, Gemini TTS) plus ImageMagick and ffmpeg post-processing. The immediate motivating need is fal.ai support for audio-driven lip-sync models that solve a silence problem in the content pipeline. But the deeper need surfaced during design review: **as media providers multiply (fal, Google, ElevenLabs, Stability, Anthropic-hosted, etc.) and each provider exposes multiple models with distinct prompt languages, callers need a unified discovery layer that preserves artificer's agnostic-transport principle.**

This design expands scope from "fal adapter" to "multi-provider architecture" because the architectural choices that get made during fal integration ossify the project's trajectory. Better to commit to the right abstraction once than to retrofit.

## Problem Statement

Two coupled problems:

**Immediate (talking-head):** The content pipeline's talking-head generator is stuck on a Veo 3.1 behavior — the model synthesizes audio along with video, ignores silence directives, caps downstream audio-split crossfade at 0.25s. Fixable only by using a video model that accepts audio as *input* (TTS URL → lip-synced video out) instead of synthesizing audio alongside video. Fal.ai hosts such models.

**Structural:** Artificer's current tool surface is provider-scoped (`gemini_generate_video`, `gemini_generate_image`, etc.) with no discovery layer. Adding fal creates inconsistency unless we simultaneously commit to the cross-provider pattern: models are logical entities with their own prompt languages, providers are access routes, the same model can be reachable via multiple providers (e.g., Veo via Google's API *or* via fal), and callers need to discover what's actually available on their configured keys.

## What Makes This Cool

- **Decoupling audio from video** (the talking-head unlock). Pipeline authors the TTS track with any cadence and silence it wants; the model animates to it verbatim. Silence problem disappears by construction.
- **Cross-provider prompt-guide reuse.** `veo_video_prompt_guide` is valuable whether the caller reaches Veo via `gemini_generate_video` or `fal_generate_video`. One guide per model, not per provider.
- **Env-key-aware discovery.** `model_catalog` reads configured API keys and returns only models the caller can actually invoke right now. No guessing, no 401s mid-pipeline.
- **No ossification.** Adding a new model is a catalog entry + a prompt guide. Adding a new provider is a new transport tool. Neither churns the rest of the surface.

## Constraints

- **Open source project already shipped.** API changes are additive. Existing `gemini_*` tools stay. Additions (transport tools, catalog, new guides) cannot break current callers.
- **MCP tool surface is LLM-readable.** Tool descriptions must carry enough signal for callers to use them without reading fal's or Google's docs out-of-band. The prompt-guide-per-model pattern carries most of that load.
- **Multi-provider is the steady state.** No forced consolidation, no hidden cross-provider routing.
- **Budget:** total validation + Phase 1 spend ≤ ~$10 across Q1 docs read, Q2 bake-off, and Phase 1 smoke. Phase 4+ budgets TBD per bake-off when those phases land.

## Terminology

- **Capability** — video, image, music, speech, safety. Top-level axis of the catalog.
- **Sub-class** — within a capability, a use-case grouping that has distinct quality criteria. Today's sub-classes: `video.talking_head`, `video.cinematic`, `image.general`, `music.general`, `speech.general`, `safety.text`. New sub-classes get added when a pipeline use case exposes a distinct criterion (e.g., `image.portrait` if a portrait bake-off reveals portrait-specific winners).
- **Model (logical)** — a logical model identity like "kling-ai-avatar-v2-pro" or "veo-3.1". The thing whose prompt language is stable across access routes. Referred to as `slug` in the catalog JSON.
- **Access route** — a specific (provider, transport tool, wire-level model id) triple through which a logical model can be invoked. A single logical model may have multiple access routes (e.g., Veo via Google or via fal).
- **Transport tool** — `fal_generate_video`, `gemini_generate_video`, etc. Thin passthrough; requires an explicit `model` param (wire-level id).
- **Non-transport tool** — `fal_classify_text`, `model_catalog`, etc. Purpose-built tools that may have deterministic defaults.

## Core Principles

The five principles that govern every decision in this design. Every subsequent section is an application of these.

1. **Transport tools require `model` explicitly.** `fal_generate_video`, `gemini_generate_video`, and future `provider_*_*` transport tools are thin passthroughs with no server-side defaults. **Non-transport tools** (safety classifiers, `model_catalog` itself, future narrow-purpose tools) may default deterministically when exactly one registered entry matches the tool's scope — but the rule is "exactly one," not "obvious choice." The moment a second entry exists, the default becomes `required`.
2. **Model selection is the caller's job, informed by the documentation layer.** Callers discover via `model_catalog`, learn via per-model prompt guides, invoke via the matching transport tool. Three-step flow, all explicit.
3. **Prompt guides are keyed by model, with per-route caveats.** The prompt *language* (what goes in the `prompt` string) is stable across access routes for the same logical model — `veo_video_prompt_guide`'s prose guidance works whether Veo is reached via Google or fal. Per-route differences exist in **request-shape** (which config knobs each provider surfaces — e.g., Google's SDK exposes `generate_audio`, `person_generation`, `seed` that fal's wrapper may not) and are documented in each guide's `Access routes` section as per-route notes.
4. **The catalog is the single source of truth for what's callable.** Static JSON in-repo, env-key filtered at read time. Every entry has a real prompt guide (or `null` for safety-class entries) and a real, registered transport tool. Entries pointing at unregistered tools are filtered out at runtime with a warning. New model = PR with catalog entry + guide + any transport updates.
5. **No routing, no taste baked into artificer.** The catalog surfaces cost and capabilities as data; the caller decides. No `generate_video(model="veo-3.1")` magic that auto-picks the cheapest access route.

## Premises (agreed 2026-04-23)

1. **Silence problem is architecturally solvable** by any audio-decoupled model. Confirmed by Q2 bake-off: Kling Avatar and veed/fabric both preserved 1s tail silence on Jenn's avatar.
2. **`extra_params: Record<string, unknown>` passthrough** is the right shape for transport tool schemas. Fal's cross-model JSON is unified; Google's SDK surface is already typed per-call. Per-model specificity lives in prompt guide tools.
3. **`@fal-ai/client` SDK is pinned.** Version bumps are explicit events with a testing checklist including re-running the relevant-capability bake-off.
4. **Multi-provider is the long-term state.** No retirement trigger for existing `gemini_*` tools. Artificer is a connection + schema + prompt-guide + discovery layer.
5. **Validation gates the build.** Per-capability bake-offs answer "which model?" before implementation commits.
6. **Use cases split within a capability.** Talking-head is not cinematic video; portrait image is not scene image. Each sub-class deserves its own bake-off when a pipeline use case emerges. The catalog structure reflects sub-classes.
7. **Cross-provider model availability is real.** Same model via multiple providers (Veo via Google or fal; Gemini image via Google or fal). The catalog surfaces all access routes for a model; callers pick explicitly.

## Scope — IN vs OUT (Commitment)

This design is a commitment to a specific scope. Items in the OUT list are not "won't do ever" — they're "won't design in this doc." Any of them becomes a separate plan file when the need is concrete.

### IN

- Core principles codified in this doc
- `fal_generate_video` transport tool (Phase 1)
- **Shared `downloadAndWrite` helper** extracted from existing Veo code, used by both Veo and fal transports (Phase 1, includes internal Veo refactor)
- **Full fal error class taxonomy** — 7 error classes (`FalValidationError`, `FalContentPolicyViolationError`, `FalGenerationTimeoutError`, `FalServerError`, `FalInfrastructureError`, `FalClientError`, `FalUnknownError`) plus body-shape dispatcher, `X-Fal-Error-Type` header reader, `.retryable` hint. Derived from fal's documented error catalog at `https://fal.ai/docs/errors`. (Phase 1)
- **`scripts/sync-fal-specs.mjs`** — build-time script that fetches both OpenAPI JSON and llms.txt per fal-hosted model in `models.json`. Writes to `src/catalog/fal-specs/{slug}/openapi.json` + `src/catalog/fal-specs/{slug}/llms.md`. Parses pricing from llms.txt into `models.json` `cost` field. Generates Zod schemas from OpenAPI to `src/generation/fal/schemas/{slug}.ts`. (Phase 1)
- `model_catalog` tool — env-key filtered, static-JSON-backed, cached at module load (Phase 2)
- Prompt-guide format spec with **per-model file split** (extract existing `veo_video_prompt_guide`, `gemini_nanobanana_prompt_guide`, `gemini_image_prompt_guide`, `gemini_tts_prompt_guide`, `gemini_lyria_prompt_guide` into `src/guides/{veo,nanobanana,imagen,gemini-tts,lyria}.ts`; `src/guides/index.ts` becomes composition registry). (Phase 2)
- New prompt guides for Phase 1 talking-head class: `src/guides/wan.ts`, `src/guides/kling-avatar.ts`, `src/guides/veed-fabric.ts`. Each imports from the committed llms.md for "Input requirements" section; hand-authored prose elsewhere. (Phase 2)
- `fal_classify_text` safety tool with deterministic single-entry auto-default (Phase 3)
- `fal_generate_image` transport tool + image-class catalog entries + per-model guides (Phase 4, gated on pipeline use case)
- `fal_generate_music` + `fal_generate_speech` transport tools + `music.*` / `speech.*` catalog entries (Phase 5, gated on pipeline use case)

### OUT (parked — separate plan if needed)

- **Dynamic catalog fetching.** No scraping fal's or Google's live model lists. Static JSON only.
- **Profiles / named preferences.** No `profile: "talking_head_default"` or similar abstraction.
- **Hidden routing / auto-select-cheapest-provider.** Transport tools never magic across providers.
- **Model deprecation automation.** No stale-guide detection, no version-bump migration helpers. Manual PR process.
- **Multi-API-key rotation, quota tracking, billing aggregation.** Out of scope.
- **General-purpose evaluation harness.** Today's `scripts/fal-bakeoff.mjs` is the MVP. Generalizing into a reusable capability-agnostic harness is a separate plan, not blocked by this one.
- **Cinematic-video bake-off, image bake-off, music bake-off, TTS bake-off.** Each is a future plan tied to a pipeline use case.

Any subsequent conversation that proposes adding an item above to this doc should be redirected to a new plan file.

## Prompt Guide Format Spec

All prompt guide tools (existing and new) conform to this structure. Enables catalog indexing and consistent caller experience.

**Tool naming:** `{model_slug}_{capability}_prompt_guide`, e.g. `veo_video_prompt_guide`, `kling_avatar_prompt_guide`, `flux_image_prompt_guide`.

**Return value structure** (prose output, sections in this order):

```
# {Model Display Name} Prompt Guide

## What this model is best for
{1-3 sentences on ideal use cases}

## Known strengths
- Bullet list

## Known weaknesses / quirks
- Bullet list

## Input requirements
- Image format, audio format, URL constraints (public vs signed), size limits
- Specific requirements that must hold for the call to succeed

## Prompt structure
{Prose guidance: shot descriptors, camera directions, style keywords, what NOT to include}

## Example prompts
{2-3 concrete examples with short captions explaining the choices}

## Access routes
{Table of {provider, tool, model_id, cost, notes} — this is machine-parseable and catalog-readable}

## Last verified
{Date + artificer version + what was verified (e.g., "prompt structure from 2026-04-23 bake-off")}
```

**Retrofits for existing guides:** `veo_video_prompt_guide` and `gemini_nanobanana_prompt_guide` get updated in Phase 2 to match this format. The `Access routes` section is the new part — previously those guides assumed Google-only access.

## `model_catalog` Tool Design

Top-level artificer tool, not scoped to any provider.

**Signature:**
```typescript
model_catalog({
  capability?: 'video' | 'image' | 'music' | 'speech' | 'safety',
  include_unavailable?: boolean,  // default false — show models whose keys aren't configured
})
```

**Return shape** (JSON):

```json
{
  "video": {
    "talking_head": [
      {
        "slug": "kling-ai-avatar-v2-pro",
        "prompt_guide": "kling_avatar_prompt_guide",
        "access_routes": [
          {
            "provider": "fal",
            "tool": "fal_generate_video",
            "model": "fal-ai/kling-video/ai-avatar/v2/pro",
            "cost": "$0.115/sec",
            "available": true,
            "key_env_var": "FAL_KEY"
          }
        ]
      },
      {
        "slug": "veed-fabric-1.0",
        "prompt_guide": "veed_fabric_prompt_guide",
        "access_routes": [
          {
            "provider": "fal",
            "tool": "fal_generate_video",
            "model": "veed/fabric-1.0",
            "cost": "$0.08/sec (480p); $0.15/sec (720p)",
            "available": true,
            "key_env_var": "FAL_KEY"
          }
        ]
      }
    ],
    "cinematic": [
      {
        "slug": "veo-3.1",
        "prompt_guide": "veo_video_prompt_guide",
        "access_routes": [
          {
            "provider": "google",
            "tool": "gemini_generate_video",
            "model": "veo-3.1-generate-001",
            "cost": "per-video pricing; see Google Cloud pricing page",
            "available": true,
            "key_env_var": "GOOGLE_API_KEY"
          },
          {
            "provider": "fal",
            "tool": "fal_generate_video",
            "model": "fal-ai/veo3.1/image-to-video",
            "cost": "per-second pricing; see fal model page",
            "available": true,
            "key_env_var": "FAL_KEY"
          }
        ]
      }
    ]
  },
  "image":  { /* Google routes seeded in Phase 2 with `stub: false`; fal routes added in Phase 4 */ },
  "music":  { /* Google route for Lyria seeded in Phase 2 with `stub: false`; fal routes added in Phase 5 */ },
  "speech": { /* Google route for Gemini TTS seeded in Phase 2 with `stub: false`; fal routes added in Phase 5 */ },
  "safety": {
    "text": [
      {
        "slug": "qwen-3-guard",
        "prompt_guide": null,
        "access_routes": [
          { "provider": "fal", "tool": "fal_classify_text", "model": "fal-ai/qwen-3-guard", "cost": "$0.002 per 1K tokens", "available": true, "key_env_var": "FAL_KEY" }
        ]
      }
    ]
  }
}
```

**Field semantics:**
- `slug` — logical model identifier, stable across access routes. Callers use this to select which model they want conceptually; they pass the route-specific `model` string to the transport tool.
- `model` (per route) — the wire-level id the transport tool needs. What the caller passes to `fal_generate_video({ model: "..." })` or `gemini_generate_video({ model: "..." })`. Different string per route even for the same logical model.
- `cost` — free-form string describing pricing. No numeric field because providers price differently (per-second, per-video, per-token, per-image, tier-based). Callers who need to compare costs programmatically parse the string or defer to the prompt guide.
- `key_env_var` — required. The env var artificer checks to compute `available`.
- `available` — `true` if `key_env_var` is set AND the transport tool is registered at runtime. Entries failing either check are excluded from the default response; include them via `include_unavailable: true`.
- `stub` (optional) — `true` indicates the entry is seeded for future activation (transport tool not yet shipped). Stubbed entries are always filtered out unless `include_unavailable: true`. Prevents Phase 2's catalog from misleading callers about capabilities that land in Phase 4/5.

**Zero-keys-configured behavior:** if no keys are set at all, return the full structure with all `available: false` and an additional `warnings: ["No API keys configured. Set FAL_KEY, GOOGLE_API_KEY, etc. and retry."]` top-level field. Covered by test.

**`extra_params` collision with the structural URL params:** if a caller passes `extra_params: { image_url: "..." }` without the tool's `image` param, the value passes through cleanly (there's nothing for the structural path to collide with). If both `image` and `extra_params.image_url` are set, the resolved `image_url` from `image` wins and a collision warning logs. Same rule for `audio` / `audio_url`. Unit test covers both cases.

**Data source:** `src/catalog/models.json` committed in-repo. Read once at tool invocation, filtered by `process.env` and by transport-tool registration, returned.

**Maintenance rule:** every catalog entry must have `prompt_guide` pointing at a registered tool (or `prompt_guide: null` only if `capability === "safety"`) and every access route must point at a registered transport tool. CI test asserts this; runtime filter protects callers against drift between `models.json` and actual tool registration.

**LLM caller flow:**
```
1. model_catalog({ capability: 'video' }) → see what's available
2. Pick a logical model based on use case (talking_head vs cinematic) + cost + availability
3. {chosen model's prompt_guide}() → learn how to prompt it
4. {chosen access route's tool}({ model, prompt, ... }) → generate
```

## Recommended Approach — Approach A, sequenced

Phase 0 (already complete as of 2026-04-23) fed the design; Phases 1–5 ship the architecture.

### Phase 0 — Architecture screen + talking-head bake-off (COMPLETE)

**Q1 (docs read, ~1 hour, $0)** — captured in `docs/plans/fal-q1-research-2026-04-23.md`. Output: shortlist of 3 audio-decoupled candidates (Wan 2.7, Kling AI Avatar v2 Pro, veed/fabric-1.0); infra posture for URL handling, concurrency, TTL, uploads.

**Q2 (scripted bake-off, afternoon, ~$5 capped)** — captured in `docs/plans/fal-bakeoff-2026-04-23.md`. Final output (revised 2026-04-24 after Wan 2.7 successful runs at explicit 720p + integer duration): **Wan 2.7 as default** (`resolution: "720p"`, `duration: Math.ceil(audio_seconds)`) for talking-head use case — rubric parity with Kling, ~25× faster warm, 13% cheaper per second. **Kling AI Avatar v2 Pro** as high-resolution fallback (native 1080p, auto-matches audio). **veed/fabric-1.0** as fast-preview / disaster-recovery path. Total bake-off spend: $2.57.

### Phase 1 — `fal_generate_video` transport tool

1. Add `@fal-ai/client` to `package.json` with **exact version pin**. Add note to `CONTRIBUTING.md`: upgrading this SDK requires re-running the talking-head bake-off against the current default model.
2. Create `src/generation/fal/client.ts` — factory, reads `FAL_KEY`, configures SDK once (module-level singleton, same pattern as Veo). Missing-key error shape matches `GOOGLE_API_KEY` error shape (test covers this). **Add to `vitest.config.ts` coverage `exclude` list** with same rationale as existing Veo client (thin SDK factory).
2a. Create `scripts/sync-fal-specs.mjs` — build-time spec ingestion. For each fal-hosted model slug in `src/catalog/models.json`: fetches `https://fal.ai/api/openapi/queue/openapi.json?endpoint_id={model-id}` → `src/catalog/fal-specs/{slug}/openapi.json`; fetches `https://fal.ai/models/{model-id}/llms.txt` → `src/catalog/fal-specs/{slug}/llms.md`; parses pricing from llms.txt `## Pricing` block and writes canonical `cost` string into `models.json`; generates Zod schemas from OpenAPI to `src/generation/fal/schemas/{slug}.ts`. Flags: `--model <slug>` (single model), `--dry-run` (print diffs, no writes). Script runs during dev setup and on demand. CI step verifies committed specs match upstream (non-zero exit on drift).
2b. Create `src/generation/fal/errors.ts` — full error taxonomy:
    - Base: `FalError extends Error` with `raw: unknown`, `status: number`, `retryable: boolean`, `errorType: string`.
    - Dispatcher: `parseFalError(response)` inspects body shape (nested `{loc,msg,type,url,ctx,input}` vs flat `{detail,error_type}`) and HTTP status, returns the appropriate subclass.
    - Subclasses: `FalValidationError` (422 nested), `FalContentPolicyViolationError extends FalValidationError` (422 with `type: content_policy_violation`), `FalGenerationTimeoutError` (504 nested, retryable=varies), `FalServerError` (500 nested, retryable=true), `FalInfrastructureError` (5xx flat, reads `X-Fal-Error-Type` header, retryable=true), `FalClientError` (499/400 flat, retryable=false), `FalUnknownError` (catchall).
    - Every subclass preserves fal's raw response body for caller inspection.
    - Derived from fal's documented error catalog at `https://fal.ai/docs/errors`. 40+ error types covered via the dispatcher-plus-catchall pattern.
3. Create `src/generation/fal/types.ts` — thin wrapper types referencing per-model schemas generated by the sync script (`src/generation/fal/schemas/{slug}.ts`). Tool-level input schema accepts `model` (string, required — no env default) plus common fields. **`poll_interval_seconds` is NOT in the schema** (fal.subscribe manages polling internally). `extra_params: z.record(z.unknown()).optional()` for passthrough. `duration_seconds` **must be integer** (regression test for today's `duration: "5"` 422 finding).
3a. Create `src/generation/utils/download-and-write.ts` — shared helper `downloadAndWrite(url: string, output: string, defaultMime = 'video/mp4'): Promise<void>`. Streams fetch response body to `getProvider(output).write()` (no full-buffer in memory). MIME from `Content-Type` header; falls back to `defaultMime`. **Rejects on Content-Type `text/html`** to prevent fal error pages being written as video. Used by both new `fal/video.ts` AND existing `generation/video.ts` (internal refactor — behavior unchanged, existing Veo integration test must still pass).
4. Create `src/generation/fal/video.ts` — `fal_generate_video` tool:
   - Accepts: `model` (string, **required** — no env default), `prompt`, `output`, `image?`, `audio?`, `duration_seconds?`, `aspect_ratio?`, `resolution?`, `negative_prompt?`, `extra_params?`, `poll_timeout_seconds?` (default 300, matching `gemini_generate_video`).
   - **Input URL resolution** (the part the Veo flow doesn't cover): fal's API accepts `image_url` and `audio_url`. For each of `image` and `audio`:
     - If already a public HTTPS URL: pass through. (Q2 confirmed `storage.googleapis.com/...` public URLs work without upload for Kling Avatar and veed/fabric. Behavior may differ per model — document per-model in the prompt guide.)
     - Otherwise, `resolveInput()` → get local path → wrap in `Blob` → `fal.storage.upload(blob)` → pass returned URL. Covers `gs://`, `s3://`, local inputs.
   - **Fal payload construction** — explicit mapping:
     | Tool param | Fal input key |
     |------------|----------------|
     | `prompt` | `prompt` |
     | (resolved image URL) | `image_url` |
     | (resolved audio URL) | `audio_url` |
     | `duration_seconds` | `duration` |
     | `aspect_ratio` | `aspect_ratio` |
     | `resolution` | `resolution` |
     | `negative_prompt` | `negative_prompt` |
     | `extra_params.*` | spread as top-level keys |

     **Key precedence:** top-level mapped keys win over `extra_params`. Collision logs a warning. Unit-tested.
     Keys the model doesn't accept are passed through to fal; fal's 4xx response is surfaced verbatim to the caller (no silent drops).
   - Call `fal.subscribe(model, { input, logs: true })`. `poll_timeout_seconds` → SDK's native `timeout` (ms). No `Promise.race` needed per Q1 research.
   - Download returned `video.url` to `output` via `getProvider(output).write()`. MIME type from HTTP `Content-Type` response header; fall back to `video/mp4`.
   - **Fal safety/content-filter rejection** surfaces as structured error with fal's reason preserved. Error shape matches Veo's `raiMediaFilteredReasons` pattern.
   - Return `{ uri: output }`.
5. Register in `src/generation/fal/index.ts` → `src/generation/index.ts` → `src/index.ts`.
6. Unit tests (Vitest, under `tests/unit/fal/`): mock `@fal-ai/client` at module boundary. Cover:
   - Payload mapping (all fields in the table above)
   - `extra_params` collision precedence + warning log
   - `extra_params.image_url` passthrough when `image` omitted (no collision)
   - Unknown key in `extra_params` passes through to fal; fal's 422 surfaces verbatim
   - **Regression (CRITICAL):** Zod schema rejects `duration: "5"` (string) as schema error; accepts integer. Today's `019dbcac-*` 422 finding.
   - **Regression:** Zod schema rejects invalid resolution values
   - Output write + MIME handling (Content-Type present, absent, `text/html` rejection)
   - `downloadAndWrite` helper tested directly (happy path, missing Content-Type, HTML rejection, provider write failure)
   - Missing-key error matches Veo error shape
   - Full error taxonomy fixture-tested per class: `FalValidationError`, `FalContentPolicyViolationError`, `FalGenerationTimeoutError`, `FalServerError`, `FalInfrastructureError`, `FalClientError`, `FalUnknownError`. Each fixture is a real fal response body.
   - Body-shape dispatcher tested: nested-vs-flat, correct subclass selected
   - `X-Fal-Error-Type` header preserved on infrastructure errors
   - HTTPS-passthrough path vs `gs://` → upload path vs local path → upload path
   - Signed GCS URL (`https://...?X-Goog-Signature=...`) passthrough behavior documented
7. Integration smokes (`INTEGRATION=1`, tests under `tests/integration/`):
   - **Kling AI Avatar v2 Pro smoke** (~$0.80): I2V with Jenn's avatar, confirm lip-sync + tail silence preservation
   - **Wan 2.7 smoke** (~$0.70): I2V with `resolution: "720p"`, integer `duration: 7`, reproduces today's findings in-tool
   - **Two-call parallel smoke** (~$1.60): concurrency verification
   - **Deliberate-failure billing probe** (Q1 unresolved item): submit invalid audio URL, verify `FalValidationError` with `type: file_download_error`, confirm fal dashboard shows no billing
   - **Veo refactor regression:** existing `tests/integration/tools.test.ts` (which covers `gemini_generate_video`) must still pass byte-identical behavior after `downloadAndWrite` extraction

### Phase 2 — Catalog + prompt guide retrofits + new talking-head guides

1. **Prompt guide format spec** (section above) committed as a convention doc: `docs/conventions/prompt-guides.md`.
2. **Veo-via-fal prompt-parity smoke** (~$1): before retrofitting `veo_video_prompt_guide` to list both Google and fal access routes, run a single cinematic-prompt generation via `fal-ai/veo3.1/image-to-video` and compare the output against a matched Google-Veo generation from the same prompt. Purpose: validate that the prompt *language* is stable across routes (Principle #3). If outputs differ materially, the retrofitted guide gets per-route prompt notes; if they match, publish with a single "Prompt structure" section and per-route notes only for request-shape.
3. **Retrofit existing guides to per-model files + format spec:** existing `src/guides/index.ts` (557 lines holding all guides) splits into individual files: `src/guides/veo.ts`, `src/guides/nanobanana.ts`, `src/guides/imagen.ts`, `src/guides/gemini-tts.ts`, `src/guides/lyria.ts`. Each updated to the format spec (adds `Access routes` section with per-route request-shape notes, e.g., "Google-route exposes `generate_audio`, `person_generation`, `seed`; fal-route does not"). `src/guides/index.ts` becomes a composition registry that imports and registers each individual guide. Content of existing guides preserved except for the format-spec additions.
4. **New talking-head guides (per-model files):**
   - `src/guides/kling-avatar.ts` → `kling_avatar_prompt_guide` — content derived from Q2 bake-off observations + `src/catalog/fal-specs/kling-ai-avatar-v2-pro/llms.md` (committed by sync script) for "Input requirements" section. Prose sections hand-authored.
   - `src/guides/veed-fabric.ts` → `veed_fabric_prompt_guide` — same pattern, plus explicit 480p vs 720p cost note from parsed llms.md pricing.
   - `src/guides/wan.ts` → `wan_video_prompt_guide` — same pattern, plus **prominent quirks warnings**: (1) `resolution: "720p"` mandatory for audio-driven mode (1080p exceeds fal's generation budget — documented 2723s inference before `generation_timeout`); (2) `duration` must be integer, and should be `Math.ceil(audio_seconds)` to cover full audio without truncation (Wan does NOT auto-match audio length); (3) wall time varies 8x between cold (63s) and warm (8s) runners.
5. **`model_catalog` tool** implementation in `src/catalog/catalog.ts`:
   - MCP tool description string: "List available media-generation models grouped by capability and sub-class. Returns logical model slugs, prompt guide tool names, and access routes (provider + transport tool + wire-level model id + cost). Call this first when you need to pick a model for a task; then call the relevant prompt_guide, then the access route's transport tool. Use `include_unavailable: true` to see models whose API keys aren't configured."
   - Reads `src/catalog/models.json` **once at module load** and caches the parsed data (filter re-runs per call against current `process.env`, since env can change, but JSON file I/O happens once). Malformed JSON surfaces as a structured error at module load with actionable message; does not crash the MCP server.
   - Filters by env keys (reads `FAL_KEY`, `GOOGLE_API_KEY`, per-route `key_env_var`) AND by transport-tool registration (drops entries whose `tool` isn't in the MCP server's registered tool list, logging a warning server-side).
   - Zero-keys-configured behavior per spec above (full structure with all `available: false` + `warnings` field).
   - `include_unavailable: true` returns everything including stubs.
   - Returns shape per spec above.
   - Unit tests (under `tests/unit/catalog/`): filter correctness per key configuration, capability param, `include_unavailable` flag, zero-keys warning, stub filtering, runtime-missing-tool filtering, **mixed-environment case** (FAL_KEY set, GOOGLE_API_KEY unset), **malformed-JSON handling**, cache read-once (second call doesn't re-read file).
6. **Seed `models.json`** with:
   - Talking-head entries for Kling AI Avatar v2 Pro, veed/fabric-1.0 — `stub: false` (transport ships in Phase 1).
   - Cinematic entries for Veo 3.1 (Google route `stub: false`, fal route `stub: false` since `fal_generate_video` ships in Phase 1).
   - Talking-head entries **ordered by default preference**: Wan 2.7 first (fal route `stub: false`, prompt-guide mandates `resolution: "720p"` + integer `Math.ceil(audio_seconds)` as `duration`), Kling AI Avatar v2 Pro second (fal route `stub: false`, auto-matches audio), veed/fabric-1.0 third (fal route `stub: false`, 480p default).
   - Image entries for Gemini 2.5 / Nano Banana (Google route `stub: false` since `gemini_generate_image` already ships). Fal image route entries are NOT seeded — they land in Phase 4 with the `fal_generate_image` transport.
   - Music entry for Lyria (Google route `stub: false` since `gemini_generate_music` already ships).
   - Speech entry for Gemini TTS (Google route `stub: false` since `gemini_generate_speech` already ships).
   - Safety entry for Qwen 3 Guard (fal route `stub: true` until Phase 3 ships `fal_classify_text`).
7. CI test (`src/catalog/catalog.test.ts`): every entry's `prompt_guide` resolves to a registered tool (or is `null` with `capability === "safety"`); every access route's `tool` resolves to a registered tool OR has `stub: true`; every route has a `key_env_var`; every route has a `cost` string. Fails the build on drift.

### Phase 3 — Safety classifier (`fal_classify_text`)

1. Create `src/generation/fal/safety.ts` → `fal_classify_text` tool.
2. Input: `text`, optional `model`, optional `categories[]`. **Default rule (non-transport tool):** if `model` is omitted AND `catalog.safety.text` contains exactly one entry, use that entry's fal-route `model` value. If the catalog has more than one safety.text entry at invocation time, `model` becomes required. Rule is deterministic and covered by test; "obvious choice" is not a factor.
3. Returns `{ safe: boolean, categories: Array<{name, confidence}>, raw: unknown }`.
4. Unit tests with fixtured harmful/safe inputs, plus tests for the one-entry auto-default AND the multi-entry-requires-model branches.
5. Integration smoke: 3 hand-picked inputs.
6. Catalog update: flip Qwen 3 Guard entry from `stub: true` to `stub: false`.
7. Pipeline-side integration into `content_vet` is out of scope (separate PR in `social-content-pipeline`).

### Phase 4 — Image generation (gated on pipeline use case)

Triggered when a concrete pipeline need emerges (blog covers, carousel backgrounds, product shots). Scope:

1. Class bake-off (portrait, landscape, product, etc. — define classes based on the triggering need).
2. `fal_generate_image` transport tool — same shape as `fal_generate_video`.
3. Prompt guides per chosen model (Flux, Seedream, Imagen-on-fal, etc.).
4. Catalog update to activate image entries.

### Phase 5 — Music + TTS (gated on pipeline use case)

Same template as Phase 4 but for `fal_generate_music` and `fal_generate_speech`. Ships when a pipeline use case specifically requires a non-Google music or TTS model.

## Open Questions

Load-bearing infra questions answered in Q1 (`fal-q1-research-2026-04-23.md`). Remaining as implementation-time items:

1. **Does `fal.storage.upload` accept a Node `Buffer` directly** or must we wrap in `Blob`? Q1 says public API types `Blob` only. Verify at Phase 1 start; wrap if needed.
2. **Default TTL for `fal.storage.upload`-ed files.** Not documented. If significant cost, add a cleanup step post-generation.
3. **Partial-failure billing** — Phase 1 deliberate-failure probe resolves this.
4. **Wan 2.7 generation timeout at default 1080p** — root-caused 2026-04-23 as a runner-timeout issue, not a dispatch issue. Phase 1 retry uses explicit `resolution: "720p"` per the bake-off doc's post-session findings. Still-queued submission `019dbcac-*` may produce the first real Wan data point. No support ticket needed unless the 720p path also times out.

## Success Criteria

- Phase 0 produces written bake-off (done).
- Phase 1 `fal_generate_video` ships with silence preservation proven in-tool against Jenn's avatar + real TTS; no regression in `gemini_generate_video`.
- Phase 2 `model_catalog` returns correct env-key-filtered results; all existing guides retrofitted to format spec; new talking-head guides land; CI catalog-integrity test passes.
- Phase 3 `fal_classify_text` ships with fixtured test + integration smoke.
- Any LLM caller can go from "I want a talking-head reel" to generation via the three-step discovery flow without reading this design doc.
- No opinion baked into artificer (no routing, no preferred-model env vars, no implicit fallbacks).

## Distribution Plan

Artificer-mcp ships via npm and GitHub Releases (release-please). Each phase ships as a minor version bump. Additive by construction — no breaking changes to existing callers. Document new env vars (`FAL_KEY`) in README alongside `GOOGLE_API_KEY`. Each phase updates README + CHANGELOG via release-please conventional commits.

## Dependencies

- `@fal-ai/client` (pinned exact version, Phase 1)
- Existing `resolveInput()`, storage provider registry (`getProvider`), and `registerTool` helpers — no changes
- No new infra dependencies

## Next Steps (immediate)

1. **Re-review this v2 design.** Adversarial check against the expanded scope.
2. **Phase 1 PR 1:** SDK pin + `src/generation/fal/client.ts` + `kling_avatar_prompt_guide` stub (so scaffolding lands before the tool).
3. **Phase 1 PR 2:** `fal_generate_video` tool + unit tests + INTEGRATION smoke.
4. **Phase 2 PR:** `model_catalog` + prompt guide retrofits + `models.json` seed + CI integrity test.
5. **Pipeline-side session** (separate repo) once Phase 1 lands.

## Decisions Log

**2026-04-23 — Sequencing: bake-off before build.** Validation gates implementation. Cost: afternoon + ~$5. Cost if skipped and premise failed: a week.

**2026-04-23 — Model-agnostic design, multi-model bake-off.** `extra_params` passthrough + model-string param means adding a fal model is config, not code.

**2026-04-23 — Per-model prompt guide tools as the specificity seam.** Fal's schema is unified; what differs per model is prompting style.

**2026-04-23 — `@fal-ai/client` pinned.** Explicit upgrades include re-running the relevant bake-off.

**2026-04-23 — Multi-provider as steady state.** Non-goal preserves Veo. No retirement trigger.

**2026-04-23 — No OpenRouter LLM proxying.** Unchanged.

**2026-04-23 — Phase renumbering vs source plan** (captured in v1). Same story in v2.

**2026-04-23 — Input URL handling: resolve-then-upload with HTTPS passthrough shortcut.** Q2 confirmed public `storage.googleapis.com/...` URLs pass through without upload for Kling Avatar and veed/fabric. Per-model behavior may vary; prompt guides document exceptions.

**2026-04-23 (v2) — Scope expansion: fal adapter → multi-provider architecture.** Initial design was fal-only. Expanded after surfacing (a) class splits within capabilities (talking-head vs cinematic) cannot be covered by a single per-capability default, (b) cross-provider access routes mean prompt guides must key by model not provider, and (c) LLM callers need a discovery mechanism to navigate the growing catalog. Expansion was gated on a named scope cap (IN/OUT list above) to prevent further creep. The core transport design (Phase 1 `fal_generate_video`) was not retrofitted by the expansion — the catalog and guide-format additions are additive.

**2026-04-23 (v2) — Transport tools require `model` explicitly.** No server-side defaults (except the safety classifier, which has one obvious choice today). Rationale: server-side defaults multiply to handle sub-class cases (talking-head vs cinematic), become brittle implicit behavior, and fight the agnostic-transport principle. Pipeline-side callers already know what they want.

**2026-04-23 (v2) — Prompt guides are keyed by model, not by provider.** `veo_video_prompt_guide` is valid for Google-hosted or fal-hosted Veo. Same prompt language regardless of access route. Existing guides get retrofitted to include an `Access routes` section.

**2026-04-23 (v2) — `model_catalog` is env-key-filtered, static-JSON-backed.** No dynamic fetching from fal / Google model lists. Every entry is a curated, tested integration with a real guide and a real tool. Trade-off: new models require a PR rather than showing up automatically. Worth it for correctness.

**2026-04-23 (v2) — Initial Q2 bake-off result: Kling AI Avatar v2 Pro as talking-head default** (on 2-of-3 data; Wan 2.7 deferred due to presumed dispatch issue). Rubric pass for both completed candidates; indistinguishable on the avatar + prompt tested. Decision based on resolution headroom, vendor stability, and purpose fit. veed/fabric-1.0 as fallback / fast-preview. **Superseded by the 2026-04-24 entry once Wan's failure was root-caused and successful runs demonstrated quality parity.**

**2026-04-24 (eng review) — Phase 1 scope expanded based on /plan-eng-review findings.**

- **Fal publishes per-model OpenAPI and llms.txt specs.** Discovered via user during review. OpenAPI at `https://fal.ai/api/openapi/queue/openapi.json?endpoint_id={model-id}` gives full input/output JSON schemas with enums/defaults/constraints; llms.txt at `https://fal.ai/models/{model-id}/llms.txt` gives human-readable pricing, tags, and overview. Decision: hybrid build-time ingestion — `scripts/sync-fal-specs.mjs` fetches both, generates Zod schemas from OpenAPI, parses pricing from llms.txt into `models.json`, commits both to `src/catalog/fal-specs/{slug}/`. Prompt guides import the llms-derived "Input requirements" section while hand-authoring the prose. Wins: schema drift detection free via PR diff, accuracy guaranteed via fal's own specs, catalog pricing stays accurate.
- **Error taxonomy expanded from 3 classes to 7.** Fal's documented error catalog at `https://fal.ai/docs/errors` has ~40 types across 4 categories with two distinct body shapes (nested PyDantic for model errors, flat for request-level). Phase 1 implements full 7-class taxonomy (`FalError` base + 6 specific + `FalUnknownError` catchall) with body-shape dispatcher. Every class preserves raw fal response body.
- **Prompt guides split to per-model files** as part of Phase 2. Existing `src/guides/index.ts` (557 lines, single file holding all guides) splits into `src/guides/{veo,nanobanana,imagen,gemini-tts,lyria,wan,kling-avatar,veed-fabric}.ts`. `index.ts` becomes composition registry. Matches the design doc's stated "one file per model" pattern; fixes internal convention mismatch flagged during review.
- **Test location follows repo convention** (`tests/unit/{domain}/`), not colocation. Design doc's `src/catalog/catalog.test.ts` corrected to `tests/unit/catalog/catalog.test.ts`. Vitest config's `include` pattern already matches.
- **`poll_interval_seconds` dropped** from `fal_generate_video` schema. Dead-code-from-birth (fal.subscribe manages polling internally) — shipping it "for parity" with Veo would ossify API surface with no caller benefit.
- **Shared `downloadAndWrite` helper extracted** into `src/generation/utils/download-and-write.ts`. Used by both Veo and fal transports. Internal refactor of `src/generation/video.ts` — no behavior change, existing integration test guards. Streams response body (no full buffer). Rejects `text/html` Content-Type to prevent fal error pages being written as video.
- **Catalog JSON cached at module load.** Re-read-per-call trivially inefficient for static data; read-once-cached + re-filter-per-call handles env-variability while eliminating file I/O cost.
- **Coverage exclusion** for `src/generation/fal/client.ts` added to `vitest.config.ts` with same rationale as existing Veo client.
- **Test gaps closed (20+):** regression test for `duration: "5"` 422 (today's finding), fixture-per-class error taxonomy tests, mixed-environment catalog tests, malformed-JSON handling, signed-URL passthrough, three-step discovery E2E flow, Wan 2.7 at 720p integration smoke.
- **TODOS.md created** with three deferred items: evaluation harness generalization, automated sync-fal-specs cron + drift PR, cost observability logging.

**2026-04-24 — Default talking-head model flipped from Kling AI Avatar v2 Pro to Wan 2.7.** After successful Wan 2.7 runs at `resolution: "720p"` + integer `duration`, the full three-way comparison gave Wan the default. Rationale: rubric parity (user-reviewed "solid, very good"), ~25× faster warm-runner wall time (8s vs 191s), ~13% cheaper per second, minor resolution trade-off (720p vs 1080p class — acceptable for reel delivery). Kling stays as the high-resolution / auto-match-audio fallback. veed/fabric-1.0 stays as fast-preview / disaster-recovery. Wan's caller-side coordination burden (pass integer `duration: Math.ceil(audio_seconds)` AND explicit `resolution: "720p"`) is documented in the prompt guide and is trivial pipeline-side since audio lengths are known at dispatch time.

**2026-04-23 (post-commit) — Wan 2.7 failure root-caused as `generation_timeout` at 1080p.** Initial framing assumed an account-dispatch issue on fal's side. Post-session inspection of the failed request (019dbc7d) returned fal's full error response: `{"type": "generation_timeout"}` after 2723.9 seconds of runner inference time on resolution=1080p audio-driven mode. Dashboard showed HTTP 504, which surfaced the clue. Workaround is explicit `resolution: "720p"` on every Wan audio-driven call; this becomes a Phase 1 transport-layer default (or a tool-description warning) and a prompt-guide mandate when the Wan guide lands. Server-side 504 appears unbilled per fal's policy — confirms partial-failure billing posture for future cases.

**2026-04-23 (v2-review) — Fixes from adversarial review (8/10 → 9/10 expected).** Applied before approval: renamed catalog's logical identifier from `model` to `slug` to remove collision with the wire-level `model` param on transport tools; reworded Principle #1 to distinguish transport tools (required `model`) from non-transport tools (deterministic single-entry default); softened Principle #3 to distinguish prompt *language* (model-keyed) from request *shape* (route-keyed); added Terminology section up front to define capability / sub-class / slug / access route / transport tool / non-transport tool; replaced `cost_per_sec_usd` numeric field with free-form `cost` string to accommodate per-video, per-token, tier-based pricing; added `stub: true/false` field to catalog entries to cleanly seed dormant future routes; specified `poll_timeout_seconds: 300` default matching Veo; added Phase 2 Veo-via-fal prompt-parity smoke to validate Principle #3 empirically before publishing the retrofitted guide; replaced Phase 3's "obvious choice" default language with a deterministic single-entry rule; added model_catalog MCP tool description string; specified zero-keys-configured behavior; specified `extra_params` URL-param pass-through semantics; specified runtime tool-registration filter protecting callers against catalog drift.

## What I noticed about how you think

- You caught the model lock-in in the first round and the use-case conflation in the second. When I framed the Phase 1 default as single-valued, you named the talking-head-vs-cinematic split immediately. Senior infra reflex — don't encode a single-valued abstraction over a multi-valued problem.
- On the catalog design, you generalized past where I was. I proposed a fal-scoped `fal_model_catalog`; you pointed out that artificer already spans Google + fal + future, and that the catalog is provider-agnostic by right. That's the kind of correction that reframes a whole design rather than just tweaking it.
- You made the right architectural call on scope expansion when it mattered. "A is key to implementing fal correctly and aligning with the design principle of artificer to begin with." That's the mode where you pay more upfront cost to avoid a retrofit later. Rare in people who ship a lot — most optimize for velocity and eat the retrofit. You priced both paths and picked the one that compounds better.
- You push back precisely. Not on everything — you accepted several of my framings without argument. But when you pushed (P1 model-agnosticism, P2 prompt-guide seam, catalog generalization), it was surgical and based on specific substance, not general skepticism. That's the signature of someone who knows which decisions are load-bearing.

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 0 | — | — |
| Codex Review | `/codex review` | Independent 2nd opinion | 0 | — | — |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 1 | CLEAR (PLAN) | 26 issues, 0 critical gaps, 0 unresolved |
| Design Review | `/plan-design-review` | UI/UX gaps | 0 | — (N/A — backend-only) | — |

**UNRESOLVED:** 0

**VERDICT:** ENG CLEARED — ready to implement. Phase 1 scope materially expanded during this review (fal spec ingestion via OpenAPI + llms.txt, 7-class error taxonomy, shared downloadAndWrite helper extraction, guide file split, test gaps closed). All decisions recorded in the Decisions Log. No design review needed (backend-only). CEO review optional — the scope expansion was architectural, not product.
