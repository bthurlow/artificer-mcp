dist/
node_modules/

# Generated by scripts/sync-fal-specs.mjs. Upstream shape is authoritative —
# reformatting would cause drift between what we commit and what fal serves.
src/catalog/fal-specs/

# Distilled from the specs above by scripts/build-fal-input-keys.mjs. The
# generator writes JSON.stringify(…, 2); prettier would collapse the short
# string arrays, so the two would fight forever and format:check could never
# pass. Freshness is enforced by `yarn catalog:keys:check`, not by style.
src/catalog/fal-input-keys.json
